import { Routes } from '@angular/router';
import { VotingViewComponent } from './features/voting-view/voting-view.component';

export const routes: Routes = [{ path: '', component: VotingViewComponent }];
